# 錠数ノート — ブラウザ内錠剤カウンター

**v0.9.0：確認済みの編集から端末内の補助AIを学習し、追加・見直し候補を提示します。黒塗り・保存・学習用書き出しに対応。自動送信や全利用者向けモデルの自動更新は行いません。使い方・学習条件は [V09.md](V09.md)。点眼ボトルの試験表記を外し、アプリ下部にソースコードZIPへのリンクを追加しました。**

スマートフォンの写真から錠剤候補を抽出し、**輪郭と通し番号で計数根拠を確認・訂正**する静的Webアプリです。TypeScript / Vite / OpenCV.js / ONNX Runtime Webを使用し、GitHub Pagesに配置できます。画像解析の外部API・画像アップロード・APIキー・アクセス解析は使用しません。

**v0.8.2：検出・編集結果を番号と右下の個数付きPNGとして保存できます。ファイル名は `2609262016_466.png` 形式です。iPhoneは共有画面の「画像を保存」、対応PCは画像フォルダーを初期位置にした保存画面を使用します。詳細は [V082.md](V082.md)。**

**v0.8.1：小さい錠剤の広いAI枠を実物の輪郭として審査していた不具合を修正。280錠の開発写真は257→280個となりました。AI補助枠を濃い青にし、番号付き画像の全画面表示・ピンチ拡大縮小・移動・×で閉じる機能を追加しました。18枚の結果と未解決の見逃しは [V081.md](V081.md) に記録しています。モデルの重みは変更していません。前版は [V08.md](V08.md) を参照してください。**

**v0.3の開発評価：開発用の実写5枚で、白錠90・袋入り丸形12・袋入り長円形36・黄色錠剤のトレー上12と36を検出。いずれも手動の正解中心との照合でFP0・FN0でした。** 改善に使った写真での成績であり、未知の写真の精度を示すものではありません。黄色錠剤の黒トレー写真も元写真で検証しています。学習済みAIモデルは付属せず、番号と数え漏れを必ず確認してください。完全に隠れた錠剤は写真から数えられません。

[公開アプリ](https://kaeru7787-hash.github.io/pill-counter/) · [GitHub](https://github.com/kaeru7787-hash/pill-counter) · [AI試験版の結果と制約](TRIAL.md)

## 起動

Node.js 24、pnpm 11を使用します。

```sh
corepack enable
corepack prepare pnpm@11.19.0 --activate
pnpm install --frozen-lockfile
pnpm dev
```

本番相当の動作（PWAを含む）：

```sh
pnpm build
pnpm preview
```

`file://` からHTMLを直接開かず、HTTPサーバー経由でアクセスしてください。依存関係とWASMはビルド成果物に含まれ、CDNから実行時に取得しません。初回に約11MBのOpenCV.jsを取得します。任意のONNXモデルとORT WASMは利用時に取得します。

## 使い方

1. 「数える対象」で錠剤または点眼ボトルを選び、「撮影」または「写真を選択」で画像を読み込みます。点眼ボトルは色付きキャップを対象とし、錠剤用AIは使用しません。
2. 写真を読み込むと解析が始まります。着色錠剤の検出経路は画像から適用を判断します。
3. 検出された全候補に輪郭と番号が付きます。参考検出数は表示中の番号の数です。
4. 「追加」で検出漏れの中心をタップ。「削除」で誤検出をタップ。「一括削除」で矩形内の候補を削除できます。「元に戻す」「やり直す」で修正を移動し、「修正をリセット」で自動検出結果に戻します。青い手動マーカーは位置の記録で、推定輪郭ではありません。
5. 「範囲指定」で矩形をドラッグすると、その範囲を再解析します。複数トレー・複数袋を個別に指定できます。範囲をまたぐ候補は要確認になります。
6. 全番号・未検出箇所を確認し、目視確認チェックを入れます。自動の信頼度は書き換えず、目視確認済みの個数を別の状態として表示します。
通常画面には解析設定・検証データを表示しません。開発者向けの `?debug=1` URLでのみ、撮影環境の切り替え・詳細設定・解析表示・保存・書き出しを利用できます。

再解析は手動修正をリセットします。修正済みなら画面内で確認します。拡大スライダー、画像内のスクロール、検出一覧からの選択も利用できます。

「番号付き画像を拡大」で、訂正後の番号を含む画像を大きく表示します。2本指で1〜8倍に拡大・縮小し、ドラッグで移動できます。＋／−、マウスホイール、「全体を表示」でも操作できます。右上の「×」またはEscapeで閉じます。この画面の操作で検出数は変わりません。濃い青の枠はAI補助、緑は画像処理、水色は手動追加です。

写真はブラウザ標準デコーダーのEXIF orientation補正後、標準で長辺最大2048pxに縮小します。「解析解像度」で3072px・元解像度を選択できますが、Safariのメモリ使用を抑えるため最大600万画素とします。表示上の縮小は解析解像度に影響しません。解像度変更はROIと手動修正をリセットします。元ファイルは変更せず保存します。HEICの対応はブラウザに依存し、開けない場合はJPEG/PNGが必要です。小さすぎる錠剤は撮影範囲を狭めて撮り直してください。

## 画像認識アルゴリズム

`src/vision` はDOMに依存せず、アプリとNode評価で同じ処理を実行します。アプリでは専用Workerを使用し、再解析・キャンセル時に終了します。

### 同程度の色・サイズを持つ着色錠剤（v0.3）

従来の背景推定は袋・机・影を巨大な前景としてつなぎ、黒トレーの高彩度除外は黄色錠剤自体を消していました。単に分離感度を上げず、前景を選ぶ段階から別経路を追加しました。

1. HSV彩度Otsuと中性背景の占有率、色相のまとまりを確認。青みなど背景の色かぶりが強い場合は、明度Otsuと画像の明度範囲から明るい領域を抽出して彩度を再評価。多色カプセルなど適用条件を満たさない画像は従来経路へ戻します。
2. 前景に支持された円弧の半径分布から代表サイズを推定します。Houghの個数は答えに使いません。
3. 推定サイズに比例する丸形・長円形の複数サイズ・角度の形状を画像と照合。内部色の被覆率と周囲との明暗差を評価し、重複形状を抑制します。大きな候補は独立した2候補で説明できる場合に再分割します。
4. 形状候補を種に、解析画像の元の解像度でOpenCV Watershedを実行。刻印・印字で分かれた小領域は距離、長短軸、面積、凸包の支持率を満たす場合だけ統合します。
5. 連結領域・形状支持Watershed・距離由来Watershedを比較し、輪郭を持つ採用候補を表示します。着色補助経路は常に「要確認」です。

着色経路の処理時間は写真によって約3〜20秒程度（開発PC）で、実機ではさらに長くなる可能性があります。形状探索は長辺960pxまでの画像、輪郭の精密化はユーザーが選んだ解析解像度を使います。色・サイズ・形状が混在する画像や似た色の背景への汎化は未確認です。この経路の自動範囲は色候補からの探索範囲で、トレー内側の認識を保証しません。手動ROIを優先します。不要な接続を作らないためこの経路にはmorphologyを適用せず、デバッグのMorphologyはThresholdと同一です。

### 黒トレー・同程度の丸形錠剤

v0.2では次の専用経路を追加しました。単独の丸形候補から十分なサイズ根拠が得られない場合は下記の非円形対応経路を使用します。

1. Otsuで暗いトレーの外輪郭を取得し、内部支持マスクを作ります。自動範囲は標準ON。手動ROIも使えます。
2. 明度Otsu、局所適応閾値、色の飽和度、トレー支持マスクを組み合わせ、机・ピンセット・高彩度ラベルを除外します。
3. 単独のコンパクトな候補の半径分布から代表サイズを推定。巨大な接触blobの面積で基準サイズを決めません。刻印の穴を埋め、スケールに合わせたcloseを適用します。
4. 距離変換のピークに、画像の円弧で支持されたHough中心を補助種として加え、marker-based Watershedで接触錠剤を分割します。**Houghの個数を答えにせず、分割後の輪郭・面積・solidity・円形度・縦横比で最終採否を決めます。**
5. A＝形状フィルター後の連結領域、B＝補助種付きWatershed、C＝別閾値の距離ピークWatershedを比較。表示個数はBの実際の輪郭数で、平均・補正係数は使いません。Houghの個数と位置も照合します。

基準画像ではB=90ですが、接触の強いA/Cは一致していないため、**90個になっても信頼度は「低・要確認」**を維持します。色の濃い錠剤、混在サイズ、角度・遠近差、強い反射には汎化を確認できていません。円形専用経路で画像の全種類を扱えるとは主張しません。

### 非円形・机・袋の汎用経路

1. **前処理**：中性色の背景に限定した穏やかなホワイトバランス、Gaussianノイズ除去。色差を壊す強い自動補正は避けます。
2. **背景推定**：周辺サンプルのLab色クラスタから優勢な背景を推定。Lと色差を組み合わせた距離画像にOtsu閾値を適用。弱い色差には適応閾値の局所コントラストも要求します。固定輝度閾値だけでは数えません。
3. **Morphology**：close/openでノイズを抑え、候補ごとのDistance Transform最大値から相対的な厚さを推定。厚さに応じたopenで袋の細い線を除去します。白飛び部分を一律除外すると白錠も失うため、広い白飛びは要確認の理由にします。
4. **輪郭・形状**：CCOMP階層を使い、囲み枠の内側にある錠剤も取得。穴を引いた面積、solidity、円形度、回転矩形の長短軸比、境界接触を調べます。ノイズの面積は画像内の候補面積分布に対する相対値で判断します。
5. **接触分離**：L2 Distance Transformの局所ピークを抑制・統合し、種領域を作ります。距離の尾根がつながるカプセルを重複カウントしないようにし、3錠の合流点にできる偽ピークも抑制。OpenCVのmarker-based Watershedで分割します。OpenCV watershedは画素間の勾配を使うため、距離をそのまま高さとして誤用せず、距離由来の種と二値境界を使用します。
6. **面積異常**：中央値の約2倍以上で形状も不自然な領域はピーク間隔を変えて再解析。結果が変われば要確認にし、面積を割り算して錠数を捏造しません。
7. **3条件照合**：A＝形状を評価した輪郭数、B＝Watershed結果、C＝異なる色差閾値・ピーク間隔での分割数。表示輪郭は原則Bです。AとCの個数が一致してBだけが異なり、B/Cの位置が安定している場合はCの実際の検出集合を選びます。個数を平均せず、BとCは位置も一対一照合します。

CLAHE、楕円フィッティング、YOLOのinstance segmentationは初版では使用していません。非円形対応は輪郭、長短軸比、距離の尾根で実装しています。印字と錠剤の意味的識別は未学習の古典CVだけでは保証できません。

### 信頼度

信頼度は**検出の一致度と警告理由**です。統計的な正解確率ではありません。

| 状態                                                                                     | 表示   |
| ---------------------------------------------------------------------------------------- | ------ |
| CV3条件とAIの個数・位置が一致し、品質警告なし                                            | 高     |
| CVのみで安定、または解析間の差が1個以内で品質警告なし                                    | 中     |
| 差が2個以上、位置不一致、接触・形状異常、白飛び、境界接触、袋、自動ROI未確認、AI失敗など | 要確認 |

0候補でも「0錠確定」にはしません。触れ合った錠剤を正しく分離できたケースも、保守的に要確認になる場合があります。CVの3条件には相関があり、一致しても全条件で同じ誤りが起き得ます。AIがない状態では「高」にしません。v0.6のAI併用中は、照合が一致しても常に「低・要確認」です。

### 自動トレー範囲とROI

丸形の黒トレー経路では暗領域の外輪郭を塗りつぶした支持マスクで外側を除外します。汎用経路の自動ROIは暗領域からの矩形推定です。初期値はONで、手動ROIを優先します。「ROI」解析表示で実際の対象範囲を確認してください。トレーと机が同じ暗色、透明トレー、背景物が多い画像は手動範囲指定が必要です。

### デバッグ

「解析表示」をONにして再解析すると、Original / ROI / Grayscale / Foreground mask / Threshold / Morphology / Distance transform / Markers / Watershed / Contours / Final detectionsを選べます。ROIの解析画像は元画像と同じ座標に配置します。

### 詳細調整

空欄は自動です。前景閾値（着色経路は彩度、黒トレー経路は明度、汎用経路はLab色差）、適応閾値の近傍幅、ぼかし幅、形態処理半径、面積上下限、円形度下限、solidity下限、Watershedピーク間隔、推定直径、Hough半径上下限・投票閾値を調整できます。面積は推定1錠に対する倍率、Hough半径は推定半径比です。px指定は表示画像でなく解析画像の座標です。着色経路の詳細指定は前景閾値・推定直径・Hough投票閾値を使用し、その他の項目は既存経路向けです。汎用経路の直径指定は面積基準に適用します。

## 精度評価・改善

```sh
pnpm test          # 信頼度、位置照合、YOLO decoder、CV、ROIなどのテスト
pnpm evaluate      # 全画像の個数と誤差、4指標、改善前後を出力
pnpm exec playwright install chromium webkit
pnpm build
pnpm test:browser  # Chromium / モバイルWebKit、手動修正・保存・ROI・オフライン
```

`tests/images/` の画像と `tests/ground-truth.json` を管理します。

```json
{ "001.png": 90, "002.png": 191, "003.png": 34 }
```

評価器はPNG/JPEG/WebPを受け付け、EXIF補正・長辺2048px以下への縮小を行います。Node側のデコードはSharpを利用するため、ブラウザと補間・色管理に僅かな違いが出る可能性があります。`tests/metadata.json` に `kind: "real"`、`scene: "tray" | "desk" | "bag"` と、必要なら誤りの分類を指定できます。実写と合成の指標はJSONの`byKind`にも分離します。正解未登録の画像があれば評価を失敗させます。`pnpm fixtures` は合成画像と正解JSONを再生成する開発用コマンドなので、実写を追加したあとに無造作に実行しないでください。

出力は `tests/reports/latest.json` と `latest.md` です。

- Exact Count Accuracy：完全一致した画像数 / 全画像数（最優先）
- MAE：絶対個数誤差の平均
- MAPE：正解が1以上の画像について絶対誤差 / 正解を平均。正解0枚は除外数を明記
- ±1 Count Accuracy：絶対誤差1以内の画像の割合

「要確認」画像を評価から除外しません。v0.3は実写5枚+合成17枚で22/22完全一致、MAE 0、MAPE 0、±1 Count Accuracy 100%。実写5枚の位置評価はそれぞれTP90/12/36/12/36、FP0・FN0。着色錠剤4枚はChromiumとモバイルWebKitでも個数・位置の両方が一致しました。これらは開発用データの結果です。iPhone実機では既存90錠写真の成功を利用者から報告いただいていますが、新しい着色錠剤写真のv0.3実機確認は未実施です。

正解中心は`tests/annotations/18-real-tray.json`、`19-bag-round.json`、`20-bag-oblong.json`、`21-yellow-tray-round.json`、`22-yellow-tray-oblong.json`に手動で記録。推論とは分離し、評価時だけ最大一対一マッチングでTP/FP/FN・Precision/Recall/F1を算出します。`tests/reports/positions/18-real-tray.png`はTP緑・FP赤・FN青です。実写テストは絶対個数誤差1以下、FP0・FN1以下を必須にします。画像上の文字・ファイル名・正解JSONを推論に渡しません。

初期の合成画像17枚では、改善前の完全一致6/17、MAE 8.294から、改善後17/17、MAE 0になりました。**これは開発に使った合成画像の回帰成績で、未知の実写での精度ではありません。** 別途、撮影端末・照明・剤形・袋の有無が異なる実写の未使用評価セットが必要です。

`baseline.json` は初期の結果、`accepted.json` は改善後に確認した回帰基準を保持します。評価時に画像ごとの誤差悪化も比較し、acceptedから悪化・画像削除すると失敗します。基準を更新する場合は、差分と輪郭を確認してください。正解個数だけではFalse PositiveとFalse Negativeの相殺を見抜けないため、原因調査には位置アノテーションと輪郭表示を併用してください。合成画像には中心座標を付属しています。現在のエラー分類はメタデータのシナリオを元にした想定分類で、自動的な原因断定ではありません。

改善履歴と残る課題は `tests/reports/improvements.md` を参照してください。

## 照明条件の追加報告とAI比較

利用者の追加報告は `tests/lighting-cases.json` に記録しています。報告時は袋入り12錠を12、長円形36錠を35（重複1・見逃し2）、密集袋86錠を81、密集トレー86錠を80と検出していました。現在は元写真4枚を取得し、向き補正・EXIF除去後の無損失PNGを実写23〜26として保存しています。スクリーンショットではなく、これらの元写真で評価しています。

既存の `22-yellow-tray-oblong.png` の36は開発者の手動中心注釈による暫定参照値です。利用者は実数不明と報告しているため、確定した正解とは区別し、評価JSONに暫定例を除いた指標も出力します。

公開前のローカル採用判断記録は `tests/reports/ensemble/adoption.md`、機械可読集計は `tests/reports/ensemble/adoption.json`、画像別結果は `comparison.json` です。新4枚はCVで12・35・81・79、AI併用で12・35・86・86。36錠画像は重複を解消し右下を回収しましたが、強い反射の下にFN1が残ります。既存90錠は90、従来成功していた袋36錠も36を維持しました。従来の22枚の成績は過去の開発用結果で、新4枚を含む現在のCV全体評価とは区別してください。

併用試験は全体＋4区画のONNX検出とCV候補を位置で照合し、複数範囲で支持される候補のみを補完します。正解個数は融合処理へ渡しません。画像・モデル・コードのハッシュを保存し、`scripts/compare-ensemble.mjs --all`、`scripts/score-ensemble.mjs`、`scripts/summarize-ensemble.mjs` で再実行できます。融合の回帰テストは `node --test tests/ensemble.test.mjs`。実モデルのブラウザ推論テストは環境変数 `PILL_AI_MODEL` を設定して `pnpm exec playwright test real-ai.spec.ts` で実行します。

AI併用の効果は確認できましたが、未知画像の独立検証・iPhone実機検証は未完了です。モデルカードMITとモデル内AGPL-3.0表記の不一致もあるため、**通常運用での精度保証は行いません**。利用者の依頼で融合処理を試験版に搭載しました。重みはリポジトリに同梱せず、設定で固定した配布元のモデルを端末が取得し、SHA-256検証後にキャッシュします。[モデル出典と条件](public/models/README.md)を参照してください。`tests/reports/ai-candidate.json` は新4枚を取得する前の旧5枚での比較履歴です。

## ローカル保存・学習用書き出し（開発者表示のみ）

IndexedDB `pill-counter-local` に、元画像のバイナリ、ファイル名、画像サイズ、解析バージョン、解析ROI・設定、自動検出、手動訂正後の輪郭・位置、目視確認状態を保存します。File/Blobの保存に関するブラウザ差を避けるため、画像はArrayBufferとして保存して読み出し時に復元します。自動保存・自動送信はしません。

「検証データを書き出す」を押すと、元画像をdata URLとして含むJSONをダウンロードします。座標は**EXIF補正後・縮小後の解析画像の画素座標**です。`analysis.width/height` と `originalWidth/originalHeight` を使って変換してください。元ファイルにはEXIFが残るため、学習データ作成側でも同じ向きに揃える必要があります。`group` フィールドを将来の色・剤形グループに利用できます。

ブラウザのデータ消去・ストレージ回収で保存内容が失われる場合があります。端末外に保管する必要がある記録は明示的に書き出してください。書き出しには元画像の袋の印字も含まれます。

## AIモデル追加

試験版には外部モデルの取得設定を付属します。重みの再配布はしません。独自のローカルモデルに差し替える場合は`modelURL`を設定から除き、次に配置します。

```text
public/models/pill-counter.onnx
public/models/config.json
```

設定例：

```json
{
  "format": "yolov8-detect",
  "inputSize": 640,
  "classes": 1,
  "scoreThreshold": 0.5,
  "iouThreshold": 0.45
}
```

現在のアダプターの契約：

- 入力：float32 `[1,3,S,S]`、RGB、0〜1正規化、ROIをアスペクト比維持でletterbox、パディング値114。
- 出力：YOLOv8型のraw検出 `[1,4+classes,N]`、`cx,cy,w,h` は入力サイズ上の画素座標、以降はクラス確率。objectness列なし。
- NMSを行い、letterboxを元座標に戻して、CVとの個数・位置を照合。
- 単一クラスのpill検出を推奨。マルチクラスでは `allowedClasses` に数えるクラスIDを指定できます（例 `[0, 3]`）。省略時は従来通り全クラス。針や異物を含むモデルでは明示してください。最大確率のクラスが対象外なら候補を除外し、次点の錠剤クラスに置き換えません。
- WASM、1スレッド。GitHub Pagesのcross-origin isolationは不要です。
- モデルがなくてもCVは動作。取得・ロード・形式エラーは要確認にします。

任意のYOLO ONNXを置くだけで互換になるわけではありません。NMS済み出力・YOLOv5/v10系・segmentationのprototype出力は別アダプターが必要です。`src/ai/onnxDetector.ts` の結果を共通 `Detection[]` に変換すれば追加できます。instance segmentationではモデルのmaskを輪郭に変換する処理を実装してください。モデルの学習・モデルファイルのライセンス・実写による精度校正は含まれません。

## GitHub Pages公開

このディレクトリをリポジトリのルートにします。GitHub上のリポジトリ作成・接続先設定は別途必要です。

1. GitHubにリポジトリを作成し、このプロジェクトを`main`へpush。
2. Settings → Pages → Build and deployment → Sourceを**GitHub Actions**に設定。
3. `.github/workflows/pages.yml` がテスト→精度評価→ビルド→ブラウザテスト→Pages deployを実行。
4. Actionsの`github-pages`環境に表示される公開URLを使用。

Viteの`base`は`./`です。プロジェクトPagesのサブパスとユーザーPagesのルートの両方で、Worker・モデル・アイコン・manifest・service workerが相対解決されます。サブパス `/pill-counter/` でもブラウザテストします。固定ベースが必要ならビルド時に`BASE_PATH=/repository-name/`を指定できます。

公開リポジトリには患者情報・薬袋の印字を含む実写や書き出しデータをコミットしないでください。ローカル検証セットは合成17枚と、向きを補正した提供実写14枚です。追加実写9枚と不採用の候補分類器は公開していません。新5枚は元JPEGでもブラウザ検証しています。

## PWA / iPhone

HTTPSのGitHub PagesをSafariで開き、共有メニューから「ホーム画面に追加」できます。manifest、PNGアイコン、service workerを同梱しています。初回のキャッシュ完了後、アプリ本体・CV・サンプルはオフラインで使用できます。AIモデルとORT WASMはオンラインで一度読み込んだ後にキャッシュされます。

更新したservice workerは既存ページが閉じられた後に有効になるため、古い画面をすべて閉じて再度開いてください。開発サーバーではservice workerを登録しません。

ターゲットは現行Safariです。WebKitのiPhone相当エミュレーションは実機そのものではありません。実機でカメラ起動・EXIF各方向・HEIC・大きな写真のメモリ使用・ホーム画面起動・オフライン・長時間使用を確認する必要があります。

## 構成

```text
src/
  main.ts                 # 軽量DOM UI、状態管理
  components/ImageViewer.ts
  image.ts                # 画像デコード・縮小
  storage.ts              # IndexedDB・JSON書き出し
  vision/                 # 前処理、マスク、輪郭、分離、照合、指標
  ai/onnxDetector.ts      # 任意のONNX検出器
  workers/visionWorker.ts
  types/index.ts
public/models/            # 任意モデル
scripts/                  # アセット、評価、合成画像、SW生成
tests/images/             # 実写14枚・合成回帰画像17枚（追加9枚はローカル検証）
tests/ground-truth.json
tests/reports/            # 精度・改善記録
.github/workflows/pages.yml
```

主な参考仕様：[OpenCV Watershed](https://docs.opencv.org/4.12.0/d7/d1c/tutorial_js_watershed.html)、[ONNX Runtime Web配布](https://onnxruntime.ai/docs/tutorials/web/deploy.html)、[GitHub Pagesワークフロー](https://docs.github.com/en/pages/getting-started-with-github-pages/using-custom-workflows-with-github-pages)。


