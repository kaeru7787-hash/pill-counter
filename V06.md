# v0.6 — AI併用と候補審査

AI併用は標準ONです。OFFは、同じ写真・同じ解析範囲で従来画像処理と比較するために残しています。解析設定・検証データは通常画面に表示せず、`?debug=1` で利用できます。

## 変更

- AI全体＋4区画の位置支持と、同じ写真の錠剤群の面積・丸さ・縦横比・色を合わせて候補ごとに採否を判定。
- 不一致の候補をすべて削除する方式ではありません。AIが見逃しても普通の錠剤らしい候補は保持。小さな断片は、別々の正常な錠剤を結合しない条件で統合。
- サイズが混在する画像、基準候補が少ない画像では、多数派の形状による除外を停止。
- 不一致・内部の明暗変化から最大3領域を選び、拡大した原画像と明るさ補正版で再照合。座標や期待個数は推論に入力しません。
- 除外候補と理由は解析結果の `rejectedCandidates` に保存。通常画面には背景除外数を表示。
- 写真は外部送信せず、既存のONNXモデルを端末内で実行。モデル本体の重みはv0.5と同一。

## 今回の元写真5枚

|画像|正解|v0.5再現値*|v0.6|FP|FN|
|---|---:|---:|---:|---:|---:|
|28錠トレイ|28|36|28|0|0|
|28錠ユニパック|28|78|28|0|0|
|56錠トレイ|56|59|56|0|0|
|56錠ユニパック|56|56|56|0|0|
|70錠トレイ|70|73|70|0|0|

* v0.5再現値は提供された元写真の無損失PNGを使ったローカル比較。利用者のiPhone画面は順に34・72・59・56・70。解像度・画像デコード・推論リサイズが同一でないため、画面の数値と混同しません。

新5枚の位置注釈はモデル候補を下書きにし、番号を付けて元画像と1錠ずつ目視照合したものです。個数は利用者確認値。独立した盲検注釈ではありません。ファイル名・袋の印字・個数ラベル・注釈座標は検出に使用しません。

## 既存画像と制約

開発用31枚（実写14・合成17）のオフライン試験で、30枚は個数完全一致。強い反射の袋入り長円形36錠写真は35錠（FP0・FN1）のままです。元の白錠90は90、黄色86錠の袋・トレイは86を維持しています。実写1枚の36錠は暫定参照値です。

局所再解析は今回の評価集合で最終個数の改善を増やしませんでした。検出の追加根拠として組み込みましたが、白飛びで失われた錠剤の復元を保証する機能ではありません。処理時間が増える場合があります。

ChromiumとモバイルWebKitで、実写14枚の位置評価とAI取得失敗・ON/OFF切替を含む30試験が通りました。新5枚は元JPEGのまま投入し、両ブラウザで全錠の位置が一致しました。強い反射の36錠画像は既知の見逃し1件を許容した試験です。モデルは同一ハッシュのローカルファイルを配信しており、実機iPhoneや外部配信先からの取得を保証する試験ではありません。

## 追加学習の採否

輪郭・相対面積・色差・内部明暗の7特徴で、小型のロジスティック分類器を実際に学習しました。実写14枚の646候補（正例586・負例60）を使い、同じ錠剤の袋・トレイ写真を同じグループにまとめた7分割の除外検証を行いました。

未学習の28錠グループで正例候補を1件除外したため、この分類器は不採用です。これらは候補レベルの指標で、最終計数の誤差とは別です。既存YOLOの重みを追加学習したという意味ではありません。今回公開するのは、既存AIと画像特徴を使った候補審査です。

学習・評価とも開発データの範囲です。追加学習モデルの採用には、別の撮影条件で収集した負例と独立した確認データが必要です。

## 再現

- `pnpm test`: 基本処理と候補審査の単体テスト。
- `PILL_AI_MODEL` に研究用のモデルを指定して `pnpm test:browser -- hybrid.spec.ts`: Chromium / モバイルWebKitで個数と位置を確認。
- `PILL_ORIGINALS` に `27-round-tray.jpeg` 等の元JPEGを置いたフォルダを指定すると、新5枚はJPEGそのものをブラウザでデコードします。
- `node scripts/run.mjs tests/hybrid.test.ts --test` の後、`node scripts/evaluate-hybrid.mjs`: 共通の推論実装とローカルCV比較記録を使用。
- `node scripts/train-candidate-rejector.mjs`: 不採用分類器の学習・グループ分割検証。

新しい元写真と画像入り評価成果物はローカルに保存し、公開リポジトリには追加していません。ブラウザ実画像試験はローカル画像・モデルがない環境では明示的にskipします。
