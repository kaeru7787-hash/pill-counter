# 最新版

v0.6の検証と制約は [V06.md](V06.md) を参照してください。AIのON/OFFは比較用として継続しています。

# v0.5 AI併用・試験版

写真選択欄の「AI併用（試験）」は標準ONです。OFFにして従来CVの結果とも比較できます。解析設定・検証データは通常画面に表示しません（開発者表示は `?debug=1`）。

AIは全体と重なる4区画を解析し、CVで未検出の候補を位置照合して追加します。小領域の二重カウントも、画像内のサイズとAIの支持がある場合だけ統合します。正解数、ファイル名、画像中の数字は推論に使いません。追加・削除・範囲指定は引き続き利用できます。

## 公開前のブラウザ検証

Chromium / モバイルWebKitで各9写真の個数・位置を検証しました。新4枚の結果は両環境で以下のとおりです。

|写真|正解|AI併用|余分な検出|見逃し|
|---|---:|---:|---:|---:|
|袋・丸形|12|12|0|0|
|袋・長円形・強い反射|36|35|0|1|
|袋・密集|86|86|0|0|
|トレー・密集|86|86|0|0|

既存の90錠写真は90（誤差0）、既存の袋36錠写真も36を維持。別のトレー長円形写真の36は手動中心注釈による暫定参照値です。

実モデルの推論・位置評価18件と通信失敗/切替2件が成功しました。実際の配布元からの取得・SHA-256検証・端末キャッシュ再利用もChromiumで確認しました。Windows版WebKitでは配布元へのTLS証明書検証が失敗し、直接取得のテストが通りませんでした。証明書検証は無効化していません。WebKitの検出試験は同一重みをローカルから提供して行っています。iPhone実機での確認はこれからです。

## モデル取得と制約

初回に端末から配布元へ約5MBのモデルを取りに行きます。写真は外部送信しません。取得後は検証済み重みを端末内に保存して再利用します。初回はネット接続が必要です。取得不能時は画面にAI失敗を表示し、従来CVの結果を「要確認」で表示します。

モデル重みをこのリポジトリに再配布していません。固定リビジョン・ハッシュ・作者・ライセンス表記の相違は[モデル情報](public/models/README.md)に記録しています。

反射下の1錠は未検出のままです。既知写真で調整した試験で、未知写真への精度は未検証です。AI併用中は常に「低・要確認」を表示します。

画像別の実行結果はローカルの `tests/reports/hybrid-browser/` に保存。新しい元写真4枚は公開リポジトリへアップロードしていません。CIでは利用可能な既存画像を評価し、外部研究モデルを指定しない場合の実モデル試験は明示的にskipします。
