# v0.11.0 自動学習

AI併用スイッチを削除し、錠剤・点眼の専用ONNXを常時使用します。モデル取得失敗時は画像処理にフォールバックし、その状態を表示します。

追加・削除・取り消し・リセットのたびに、その写真の最終補正から端末専用の外観分類器を学習します。18次元の色情報・濃淡・周辺との関係を、正例／追加例／除外例の学習済み中心へ圧縮します。次の画像、pagehide、非表示時に反映し、強制終了でイベントが来ない場合は次回起動時に学習済みチェックポイントを反映します。目視確認チェックや保存ボタンは学習の条件ではありません。

推論時は専用ONNXと画像処理の後に、この学習済み分類器で除外と追加を行います。追加は未採用候補だけでなく、手動追加から学習したサイズで画像を走査します。錠剤・点眼は別モデルです。未知の外観、正負が競合する外観は元の検出を優先します。二重マークの削除は近くの正例を除外例として学習しません。

これは実際にパラメーターを更新して検出へ反映する端末内の追加学習であり、YOLO/ONNX全体の再訓練や全利用者共通のクラウド学習ではありません。少数の補正で未知の形状すべてに汎化することは保証しません。同じ端末・ブラウザ内で継続します。

保存するのは上限付きのモデル係数（各種32中心、計96中心）と更新管理情報です。写真・切り抜き・全画素・検出座標の教師データは保存しません。旧版で明示保存された学習記録は係数を永続化してから元画像ごと削除します。失敗したら削除せず状態を表示します。旧アプリキャッシュはService Worker更新時に削除し、ONNX/PTの二重キャッシュを作りません。現在の画面・オフライン起動・次回推論に必要なデータ、学習済み係数、ユーザーが書き出した結果画像は保持します。

補正の取り消しは同じ写真の直近モデル寄与を置換し、重複した終了通知で二重学習しません。写真の切替時は旧キャンバスを解放します。補正情報・画像の外部送信はありません。

## 検証

- 単体：位置を移した画像への追加・除外、候補外の見逃し走査、二重マーク削除、ROI、undoによるモデル置換、再起動復旧、容量エラー、モデルサイズ制限。
- ブラウザ：実際の編集→次の別画像への除外反映、終了・再起動、学習画像を残さないこと、旧記録移行、容量エラー。ChromiumとモバイルWebKitで確認。
- 既存の検出・画像編集・画像出力・拡大・漫画の回帰テスト。

合成画像テストは自動学習経路が動く根拠であり、未知の実写での精度向上率ではありません。基礎ONNXモデルの重みはv0.10.0と同じです。
