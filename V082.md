# v0.8.2 結果画像の保存

個数表示の直下に「結果画像を保存」を追加しました。現在の手動追加・削除を反映した輪郭と通し番号をPNG画像に出力し、右下に濃紺の下地・白文字・白い縁で個数を表示します。錠剤は「錠」、点眼ボトルは「本」です。選択中の黄色表示、ROI、解析用レイヤーは含めません。

ファイル名は保存ボタンを押した端末のローカル日時（年下2桁・月・日・時・分）と現在の個数です。例: `2609262016_466.png`。同じ分に同じ個数を出力すると同名になるため、上書き確認や連番は保存先の動作に従います。

## 保存先

- iPhone / iPad: 共有画面の「画像を保存」を選んで写真アプリに保存します。OS側の保存操作や写真ライブラリーでのファイル名の扱いはアプリから強制できません。
- 保存先選択に対応するPCブラウザー: 「画像 / Pictures」フォルダーを初期位置として保存画面を開きます。利用者が保存先を選択します。
- 上記に対応しないブラウザー: PNGをダウンロードします。保存先はブラウザーの設定に従います。保存画面を開けない場合は「ファイルとして保存」を表示します。

画像は端末内で作成します。共有先はOSの共有画面で利用者が選択します。保存をキャンセルしても編集結果は変わらず、勝手にダウンロードへ切り替えません。

## 検証

ChromiumとモバイルWebKitで、画像保存6件と既存の拡大・バージョン表示4件が通過。PNGの実データを読み取り、640×480の画像、右下の下地と白文字、手動追加マーカー、編集後25個・取り消し後24個のファイル名を確認しました。保存先選択と共有APIは模擬し、PNGファイル、pictures指定、ユーザー操作の有効性、キャンセル、失敗時の明示的なダウンロードを検査しました。

追加で提供写真の466錠と52本を両環境で解析し、1個削除して465錠・51本として出力する4件も通過しました。出力画像の右下表示を目視確認しています。型検査・本番ビルドも通過しました。

iPhone実機の写真アプリへの保存は未検証です。認識処理はv0.8.1から変更していません。認識精度の記録は[V081.md](V081.md)を参照してください。
