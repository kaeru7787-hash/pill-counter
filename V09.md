# v0.9.0 確認済み編集からの学習補助

## 利用者の操作

1. いつも通り計数し、追加・削除を行い、画像全体を確認する。
2. 「すべての番号と数え漏れを目視確認した」にチェックする。
3. 「精度向上に協力・学習補助（試験）」を開き、確認済みの結果を保存する。
4. 必要なら画像をドラッグして氏名や処方番号を黒塗りする。解析範囲外も黒塗りされる。撮影セット名に個人情報を入力しない。同じ薬品を並べ替えたり撮り直した写真は同じ撮影セット名にする。
5. 削除した枠の理由を分類する。「薬品以外・反射だった」だけを背景例として使う。重複や未分類の削除は背景の正解にしない。内容を確認して端末内に保存する。
6. データがそろったら「保存データから学習する」を押す。錠剤・点眼ボトルは、現在表示している解析結果の対象ごとに別々に学習する。
7. 採用基準を満たした場合、「学習した補助で見直す」を使う。追加候補は画像を見て採用し、見直し候補は通常の編集機能で確認する。

学習操作も候補表示も現在の個数を自動変更しない。元のONNX検出モデルと計数ロジックは従来通りで、今回追加するのは端末内で重みを学習する補助分類器。点眼ボトルの選択肢と説明から試験モードという表記を外した。学習補助自体は試験扱いとする。

## 実際に学習するもの・採用条件

画像の中心・内周・周辺のRGB、明るさのばらつき、彩度、白飛び率を18個の特徴として、正則化したロジスティック分類器をWeb Workerで学習する。確認済みの薬品と、明示的に分類した背景の区別を学ぶ。手動追加の枠はサンプリング用の仮の大きさであり、物体の正確な輪郭ではない。書き出した中心位置は将来の検出器学習の注釈材料にできるが、YOLOの正解枠へ無条件変換しない。

- 独立した撮影セット4組以上、薬品20例以上・背景8例以上が最低条件。これは精度保証に必要な枚数という意味ではない。
- 同一解析画素のSHA-256で同じ写真の登録を更新し、再投稿を別の検証画像にしない。リサイズや別角度の写真はハッシュで同一判定できないので撮影セット名でまとめる。
- 撮影セット単位の交差検証（最大5分割）を行い、同じ組の写真を学習と評価にまたがせない。
- 写真ごとの最大使用例は薬品80・背景40。同じ写真の大量の錠剤だけに偏らないよう写真・クラス別に重みを付ける。
- 高い確信の判断（0.9以上／0.1以下）だけを提案対象にし、それ以外・学習範囲から外れた見た目は保留する。スコアは校正済みの正答確率ではない。
- 検証時の高い確信の誤答が1件でもある場合、または正答した薬品5例・背景3例に届かない場合は採用しない。基準を通っても新しい実写での正確さは保証しない。
- 未採用の既存検出候補を再評価するため、元の検出処理が候補を作れなかった薬品は補えない。横倒しボトルを立体認識する新しい検出モデルではない。
- 自動計数への昇格は行わない。データ更新・削除時は対応するモデルを解除する。モデルはタブ内のメモリーにあり、再読み込み時は保存データから再学習する。

## データとプライバシー

別のIndexedDB `pill-counter-learning` に、黒塗り後のPNG、中心位置、確認済み個数、削除理由、特徴データ、撮影セット名、解析情報を保存する。PNGは向き補正・解析解像度のキャンバスから再生成し、元ファイル名やEXIFは持ち込まない。黒塗りにかかるサンプリング領域は特徴抽出から除く。中心位置も隠した領域や解析範囲外は除く。範囲外と黒塗り領域は「背景」ではなく未知領域であり、全画像の負例として使ってはいけない。

保存は1回ずつ明示的な確認が必要。編集すれば目視確認は解除される。40枚・画像合計80MBを上限とし、保存失敗を表示する。保存済み画像は確認・個別削除できる。通常の検証データ保存とは別であり、従来の保存データは変更しない。

写真や特徴の自動送信は一切ない。共有サーバーは未設定。利用者が確認して書き出すJSONに画像を含むので、提供する場合は非公開の方法で開発者へ渡す。GitHubの公開Issue・公開リポジトリへ自動投稿しない。

## 開発者が提供データを再学習する

内容と撮影セット分けを人が確認したファイルを使う。入力は実行時に指定し、公開リポジトリへ置かない。

```powershell
$env:LEARNING_INPUT = 'C:/private/pill-learning-2026-09-26.json'
$env:LEARNING_OUTPUT = 'work/learning-evaluation'
pnpm learning:evaluate
```

対象ごとの検証結果と、基準を通った場合だけ候補モデルを出力する。入力のサイズ・スキーマ・特徴値を検査し、ネットワーク送信や本番配信はしない。失敗時は古いモデル候補を残さない。写真の内容や利用者のラベルが真実かまでは自動確認できないので、公開モデルを作る前に別途レビューと独立した実写評価が必要。

将来の全利用者向け学習には、非公開の収集先、提供者の同意・削除管理、管理者の採用確認、検出モデルの再学習環境、独立評価、配信とロールバックを追加する。本版の端末内学習と混同しないこと。

## 配布・ソースコード

アプリ下部の「ソースコードZIP」はGitHubの最新mainのアーカイブを取得する。更新時は同じリポジトリの該当コミットのZIPも案内する。ZIPを展開しNode.jsとpnpmを用意して `pnpm install --frozen-lockfile`、`pnpm build` でビルドできる。GitHub Actionsが型検査・単体試験・ブラウザー試験を実行する。

学習用の利用者データ、ローカル実験画像、作業フォルダー、依存パッケージ本体、ONNX重みは今回のソース配布に追加しない。既存モデルの出典と未解決のライセンス表記の相違は [public/models/README.md](public/models/README.md) に記載。独自モデルへ差し替える場合も元のモデル条件を確認する。

## 検証の範囲

単体試験で実際の学習、撮影セット分離、データ不足・矛盾時の不採用、背景理由の区別、黒塗り範囲の除外、件数を変えない候補提示を検査する。ブラウザー試験では確認必須、PNGの黒塗り・EXIF除去、端末内の永続化・同じ写真の更新・削除、JSON書き出し、Web Worker学習、修正時の確認解除をChromiumとモバイルWebKitで検査する。

学習の動作試験は合成データによるもの。利用者が提供する未知の実写で精度が向上したという測定結果はまだない。
